// Only change code below this line
const increment = (number, value = 1) => number + value;
// Only change code above this line

// = 뒤에는 값이 들어가지 않았을 때 기본값을 의미함

console.log(increment(5, 3)); // 8
console.log(increment(5));    // 1