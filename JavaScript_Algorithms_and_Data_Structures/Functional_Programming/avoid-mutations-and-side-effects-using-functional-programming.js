// 전역변수인 fixedValue의 값은 고정된 상태로 fixedValue 보다 1큰 값을 리턴.
// 전역변수의 값은 변경하지 않고 리턴.

// The global variable
let fixedValue = 4;

function incrementer() {
  // Only change code below this line
let increatedValue = fixedValue;
return increatedValue + 1;

  // Only change code above this line
}